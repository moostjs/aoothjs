node_modules
package-lock.json
packages/**/dist
dist
temp
coverage
.npmrc
docs/.vitepress/cache
docs/.vitepress/dist
*.tgz
*.log
.DS_Store
TODO.md
.claude
.agents
explorations
.playwright-mcp
.playwright-cli
*.png

# Atscript runtime output — bundler transforms .as on the fly via unplugin-atscript
*.as.js
*.as.d.ts
src/atscript.d.ts
**/src/atscript.d.ts
TODO.md

# Local design drafts — kept on disk for reference, NEVER committed (may name
# internal/external products). Scrub before any goes into docs/skills/source.
# Anchored to repo root: macOS git runs core.ignorecase=true, so an unanchored
# `IDP.md` would also match `docs/api/idp.md`, `references/idp.md`, etc.
/IDP.md
/AUTH-SERVER.md
/rfc-credential-claims-arbac-bridge.md
/REFRESH_TOKEN.md
/AUTHZ_CONSENT.md
